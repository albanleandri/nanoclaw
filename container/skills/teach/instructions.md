When the user explicitly invokes `teach`, load and follow the full
`teach/SKILL.md` and its referenced templates. This is a user-invoked skill;
do not activate it merely because its instructions are visible here.
