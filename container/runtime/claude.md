## Claude memory

`CLAUDE.local.md` in `/workspace/agent/` is this group's persistent Claude memory. Keep universally relevant user preferences and recurring context there in a short, structured form.

For substantive information that does not belong in every turn, create an appropriate structured file under `/workspace/agent/` and add a concise pointer in `CLAUDE.local.md`. Evolve the organization as the information grows; split very large files and maintain an index.

Before the first substantive reply in a session, consult `CLAUDE.local.md` and any advertised shared resource relevant to the request. Apply stored binding preferences without requiring the user to repeat them.
